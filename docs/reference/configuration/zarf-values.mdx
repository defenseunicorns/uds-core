---
title: Zarf values
description: Reference for overriding UDS Core configuration with native Zarf values, including the per-package value keys, defaults, and how values relate to bundle variables.
sidebar:
  order: 3.004
---

UDS Core exposes a curated set of Helm configuration as native [Zarf values](https://docs.zarf.dev/ref/examples/values-templating/), so you can override common settings with a values file when deploying a Core package directly with `zarf package deploy`, without building a UDS bundle. Bundle `variables` and `values` continue to work unchanged; Zarf values are an additive option for the straight-Zarf deployment path.

## Overriding values at deploy

Pass a values file with `--values` (repeatable) when deploying a Core package. You can also set individual values inline with `--set-values key.path=value`. Keys are namespaced by the chart they target. Only the keys you set are applied; everything else keeps its chart default.

```yaml title="values.yaml"
# Scale authservice and enable Keycloak HA
authservice:
  replicaCount: 3
keycloak:
  autoscaling:
    enabled: true
```

```bash
uds zarf package deploy zarf-package-core-amd64-x.x.x.tar.zst --values values.yaml --confirm
```

> [!NOTE]
> Zarf deep-merges values onto the chart defaults. Object overrides preserve sibling defaults; list values (for example, a gateway's `service.ports`) replace the chart default entirely, so include the full list when overriding one.

## Available values

The full UDS Core (`standard`) package exposes the union of every layer's values. The individual layer packages (`core-base`, `core-identity-authorization`, `core-logging`, `core-monitoring`) each expose their own subset. Each package validates its values against a JSON schema, so unknown keys are rejected at deploy time.

### Base (`core-base`)

This package is the UDS Core foundation layer, including the Pepr operator, Istio service mesh, Envoy Gateway, and supporting CRDs. The Zarf values surface exposes resource and gateway settings for Pepr and Istio. The gateway `name` and `domain` are excluded from the `uds-istio-config` maps (`name` is bound to the `Gateway` resource and its Deployment selector; `domain` is driven by the `DOMAIN` Zarf variable).

| Field | Type | Default | Description |
|---|---|---|---|
| `pepr.watcher.resources` | object | chart default | Resource requests and limits for the Pepr watcher pod |
| `pepr.admission.resources` | object | chart default | Resource requests and limits for the Pepr admission pods |
| `istiod.resources` | object | chart default | Resource requests and limits for Istiod |
| `uds-istio-config-admin.tls.cert` | string | unset | Base64-encoded TLS certificate for the admin gateway |
| `uds-istio-config-admin.tls.key` | string | unset | Base64-encoded TLS key for the admin gateway |
| `uds-istio-config-admin.tls.supportTLSV1_2` | boolean | `false` | Allow TLS 1.2 on the admin gateway |
| `uds-istio-config-tenant.tls.cert` | string | unset | Base64-encoded TLS certificate for the tenant gateway |
| `uds-istio-config-tenant.tls.key` | string | unset | Base64-encoded TLS key for the tenant gateway |
| `uds-istio-config-tenant.tls.supportTLSV1_2` | boolean | `false` | Allow TLS 1.2 on the tenant gateway |
| `tenant-gateway.service` | object | chart default | Tenant gateway LoadBalancer service config, including `ports` for non-HTTP(S) traffic |

> [!NOTE]
> The Istio sidecar proxy resource requests (`global.proxy.resources.requests`) are exposed through the existing Zarf `variables` (`PROXY_MEMORY_REQUEST`, `PROXY_CPU_REQUEST`), not through values, to avoid a double mapping.

### Identity and authorization (`core-identity-authorization`)

This package exposes Keycloak and authservice settings. The `keycloak` key maps to the Keycloak chart broadly; `image`, `configImage`, the `Service`, `domain`/`adminDomain`, security contexts, and monitoring objects (`ServiceMonitor`/`PrometheusRule`) are excluded so they cannot be overridden.

| Field | Type | Default | Description |
|---|---|---|---|
| `keycloak.insecureAdminPasswordGeneration.enabled` | boolean | `false` | Generate an insecure admin password for dev/test |
| `keycloak.autoscaling.enabled` | boolean | `false` | Enable Keycloak HA (horizontal autoscaling) |
| `keycloak.devMode` | boolean | chart default | Enable Keycloak dev mode |
| `keycloak.resources` | object | chart default | Resource requests and limits for Keycloak |
| `keycloak.postgresql.host` | string | `""` | External PostgreSQL host |
| `keycloak.postgresql.username` | string | `""` | External PostgreSQL username |
| `keycloak.postgresql.password` | string | `""` | External PostgreSQL password |
| `keycloak.postgresql.database` | string | `""` | External PostgreSQL database name |
| `keycloak.postgresql.port` | integer | `5432` | External PostgreSQL port |
| `authservice.replicaCount` | integer | `1` | Number of authservice replicas |
| `authservice.resources` | object | `{}` | Resource requests and limits for authservice |

> [!WARNING]
> External PostgreSQL requires `keycloak.devMode: false`. The Keycloak chart fails at deploy time when any `postgresql.*` field is non-empty and `devMode` is `true` (the chart default). Always set `keycloak.devMode: false` alongside any `keycloak.postgresql.*` override.

> [!NOTE]
> `keycloak.domain` and `keycloak.adminDomain` are not exposed as values; they remain driven by the `DOMAIN` and `ADMIN_DOMAIN` Zarf variables.

### Logging (`core-logging`)

This package includes Loki and Vector (log collection and forwarding). The Zarf values surface exposes Loki storage and replica settings.

| Field | Type | Default | Description |
|---|---|---|---|
| `loki.loki.storage` | object | chart default | Loki object storage config, including `bucketNames` and `s3` connection settings |
| `loki.write.replicas` | integer | `3` | Loki write component replicas |
| `loki.read.replicas` | integer | `3` | Loki read component replicas |
| `loki.backend.replicas` | integer | `3` | Loki backend component replicas |

### Monitoring (`core-monitoring`)

This package includes the full Prometheus/Grafana observability stack (kube-prometheus-stack, Prometheus Blackbox Exporter, Grafana). The Zarf values surface exposes Grafana database and scaling settings. The `uds-grafana-config` key maps to the Grafana config chart broadly; `domain`/`adminDomain` and `sso` are excluded (`sso` is wired to Keycloak by UDS, so overriding it would break authentication).

| Field | Type | Default | Description |
|---|---|---|---|
| `uds-grafana-config.postgresql.host` | string | `""` | External PostgreSQL host |
| `uds-grafana-config.postgresql.database` | string | `""` | External PostgreSQL database name |
| `uds-grafana-config.postgresql.user` | string | `""` | External PostgreSQL username |
| `uds-grafana-config.postgresql.password` | string | `""` | External PostgreSQL password |
| `uds-grafana-config.postgresql.ssl_mode` | string | `require` | PostgreSQL SSL mode |
| `uds-grafana-config.postgresql.port` | integer | `5432` | External PostgreSQL port |
| `grafana.autoscaling.enabled` | boolean | `false` | Enable Grafana HA (horizontal autoscaling) |

> [!WARNING]
> External PostgreSQL configuration requires `host`, `database`, `user`, `password`, and `port` to be set together. Setting any one of `host`, `database`, `user`, or `password` without the others causes a hard failure at deploy time.

> [!NOTE]
> `uds-grafana-config.domain` and `uds-grafana-config.adminDomain` are not exposed as values; they remain driven by the `DOMAIN` and `ADMIN_DOMAIN` Zarf variables.

## Relationship to bundle variables

Zarf values do not replace the bundle `variables` and `values` that UDS bundles use. Both mechanisms coexist:

- Deploying a Core package directly with `zarf package deploy --values values.yaml` uses Zarf values. This is the path these values are designed for.
- Deploying through a UDS bundle continues to use the bundle's `variables` and `values`. If a bundle variable and a Zarf value target the same Helm path, the bundle variable wins, because `###ZARF_VAR###` templating runs after values are applied.

Avoid setting the same Helm path through both mechanisms in a single deploy.

## Related documentation

- [Zarf values templating](https://docs.zarf.dev/ref/examples/values-templating/) - upstream Zarf documentation for the values feature
- [Package Values](/concepts/configuration-and-packaging/package-values/) - how UDS Packages author and expose Zarf values
- [Loki storage](/reference/configuration/loki-storage/) - bundle override reference for Loki object storage
- [Identity and authorization](/reference/configuration/identity-and-authorization/) - Keycloak configuration reference
