# Copyright 2024-2026 Defense Unicorns
# SPDX-License-Identifier: AGPL-3.0-or-later OR LicenseRef-Defense-Unicorns-Commercial

includes:
  - utils: utils.yaml
  - test: test.yaml
  - create: create.yaml
  - deploy: deploy.yaml
  - setup: setup.yaml

variables:
  - name: FLAVOR
    default: upstream

  - name: VERSION
    description: "The version of the packages to build"
    # x-release-please-start-version
    default: "1.12.0"
    # x-release-please-end

  - name: LAYER

tasks:
  - name: standard-package
    description: "Publish the UDS package"
    actions:
      - task: utils:determine-repo
      - description: "Sign, verify, and publish amd64/arm64 packages per flavor"
        shell:
          darwin: bash
          linux: bash
        cmd: |
          echo "Publishing package to ${TARGET_REPO}"
          if [ "${SNAPSHOT}" = "true" ]; then
            signing_identity_regex='^https://github\.com/defenseunicorns/uds-core/\.github/workflows/publish\.yaml@refs/heads/main$'
          else
            signing_identity_regex='^https://github\.com/defenseunicorns/uds-core/\.github/workflows/publish\.yaml@refs/tags/v[0-9]+\.[0-9]+\.[0-9]+$'
          fi

          packages=(
            build/zarf-package-core-amd64-${VERSION}.tar.zst
            build/zarf-package-core-arm64-${VERSION}.tar.zst
          )

          for package in "${packages[@]}"; do
            ./uds zarf package sign "${package}" --keyless --confirm
            ./uds zarf package verify "${package}" \
              --certificate-identity-regexp "${signing_identity_regex}" \
              --certificate-oidc-issuer "https://token.actions.githubusercontent.com"
            ./uds zarf package publish "${package}" oci://${TARGET_REPO}
          done

      - description: "Tag the latest package (if a snapshot release)"
        cmd: |
          if [ $(echo "${TARGET_REPO}" | grep 'snapshot') ]; then
             pkgPath="${TARGET_REPO}/core"
             ./uds zarf tools registry copy ${pkgPath}:${VERSION}-${FLAVOR} ${pkgPath}:latest-${FLAVOR}
          fi

  - name: checkpoint-package
    description: "Publish the UDS checkpoint package"
    actions:
      - description: "Publish the checkpoint package for the current UDS_ARCH"
        cmd: |
          ./uds zarf package publish build/zarf-package-k3d-core-slim-dev-${UDS_ARCH}-${VERSION}.tar.zst oci://ghcr.io/defenseunicorns/dev/uds/checkpoints

  - name: checkpoint-latest
    description: "Tag the latest UDS checkpoint package"
    actions:
      - description: "Tag the multi-architecture checkpoint package as latest"
        cmd: |
          pkgPath="ghcr.io/defenseunicorns/dev/uds/checkpoints/k3d-core-slim-dev"
          ./uds zarf tools registry copy ${pkgPath}:${VERSION} ${pkgPath}:latest

  - name: bundles
    description: "Publish UDS Bundles"
    actions:
      - task: utils:determine-repo
      - description: "Publish amd64 and arm64 legacy bundles"
        cmd: |
          echo "Publishing legacy bundles to ${TARGET_REPO}"
          ./uds publish bundles/k3d-standard/uds-bundle-k3d-core-demo-amd64-${VERSION}.tar.zst oci://${TARGET_REPO}/bundles --no-progress
          ./uds publish bundles/k3d-standard/uds-bundle-k3d-core-demo-arm64-${VERSION}.tar.zst oci://${TARGET_REPO}/bundles --no-progress

          ./uds publish bundles/k3d-slim-dev/uds-bundle-k3d-core-slim-dev-arm64-${VERSION}.tar.zst oci://${TARGET_REPO}/bundles --no-progress
          ./uds publish bundles/k3d-slim-dev/uds-bundle-k3d-core-slim-dev-amd64-${VERSION}.tar.zst oci://${TARGET_REPO}/bundles --no-progress

      - description: "Publish amd64 and arm64 UDS CLI Next bundles"
        cmd: |
          echo "Publishing UDS CLI Next bundles to ${TARGET_REPO}"
          CLI_FEATURES=NextMode=true ./uds bundle push bundles/k3d-standard-next/uds-bundle-k3d-core-demo-next-amd64-${VERSION}.tar.zst oci://${TARGET_REPO}/bundles/k3d-core-demo-next:${VERSION}
          CLI_FEATURES=NextMode=true ./uds bundle push bundles/k3d-standard-next/uds-bundle-k3d-core-demo-next-arm64-${VERSION}.tar.zst oci://${TARGET_REPO}/bundles/k3d-core-demo-next:${VERSION}

          CLI_FEATURES=NextMode=true ./uds bundle push bundles/k3d-slim-dev-next/uds-bundle-k3d-core-slim-dev-next-amd64-${VERSION}.tar.zst oci://${TARGET_REPO}/bundles/k3d-core-slim-dev-next:${VERSION}
          CLI_FEATURES=NextMode=true ./uds bundle push bundles/k3d-slim-dev-next/uds-bundle-k3d-core-slim-dev-next-arm64-${VERSION}.tar.zst oci://${TARGET_REPO}/bundles/k3d-core-slim-dev-next:${VERSION}

      - description: "Tag the latest bundles"
        cmd: |
          pkgPath="${TARGET_REPO}/bundles/k3d-core-demo"
          ./uds zarf tools registry copy ${pkgPath}:${VERSION} ${pkgPath}:latest
          pkgPath="${TARGET_REPO}/bundles/k3d-core-demo-next"
          ./uds zarf tools registry copy ${pkgPath}:${VERSION} ${pkgPath}:latest
          pkgPath="${TARGET_REPO}/bundles/k3d-core-slim-dev"
          ./uds zarf tools registry copy ${pkgPath}:${VERSION} ${pkgPath}:latest
          pkgPath="${TARGET_REPO}/bundles/k3d-core-slim-dev-next"
          ./uds zarf tools registry copy ${pkgPath}:${VERSION} ${pkgPath}:latest

  - name: single-layer
    description: "Test and Publish UDS Core layer"
    actions:
      - task: test:layer-dependencies
      - task: create:single-layer
        with:
          layer: ${LAYER}
      - task: deploy:single-layer
        with:
          layer: ${LAYER}
      - task: test:validate-package
        with:
          layer: ${LAYER}
      - task: utils:determine-repo
      - description: "Sign, verify, and publish build of layer"
        shell:
          darwin: bash
          linux: bash
        cmd: |
          if [ "${SNAPSHOT}" = "true" ]; then
            signing_identity_regex='^https://github\.com/defenseunicorns/uds-core/\.github/workflows/publish\.yaml@refs/heads/main$'
          else
            signing_identity_regex='^https://github\.com/defenseunicorns/uds-core/\.github/workflows/publish\.yaml@refs/tags/v[0-9]+\.[0-9]+\.[0-9]+$'
          fi

          package="build/zarf-package-core-${LAYER}-${UDS_ARCH}-${VERSION}.tar.zst"
          ./uds zarf package sign "${package}" --keyless --confirm
          ./uds zarf package verify "${package}" \
            --certificate-identity-regexp "${signing_identity_regex}" \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com"
          ./uds zarf package publish "${package}" oci://${TARGET_REPO}
