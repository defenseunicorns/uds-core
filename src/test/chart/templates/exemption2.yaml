apiVersion: uds.dev/v1alpha1
kind: Exemption
metadata:
  name: podinfo2
  namespace: uds-policy-exemptions
spec:
  exemptions:
    - policies:
        - RequireNonRootUser
      title: "podinfo1"
      matcher:
        namespace: podinfo
        name: "^podinfo.*"
