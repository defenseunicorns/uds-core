# Copyright 2024 Defense Unicorns
# SPDX-License-Identifier: AGPL-3.0-or-later OR LicenseRef-Defense-Unicorns-Commercial

# Values set to intentionally violate pepr policies
securityContext:
  runAsUser: 0
  runAsGroup: 0
  runAsNonRoot: false

service:
  enabled: true
  # Using a Load Balancer (rather than a NodePort) avoids port conflicts on the host.
  type: LoadBalancer
