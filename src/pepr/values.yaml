watcher:
  serviceMonitor:
    enabled: ###ZARF_VAR_SERVICE_MONITORS###
  envFrom:
    - secretRef:
        name: uds-operator-config
admission:
  serviceMonitor:
    enabled: ###ZARF_VAR_SERVICE_MONITORS###
  envFrom:
    - secretRef:
        name: uds-operator-config
