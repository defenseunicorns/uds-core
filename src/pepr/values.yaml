watcher:
  serviceMonitor:
    enabled: ###ZARF_VAR_SERVICE_MONITORS###
  envFrom:
    - secretRef:
        name: uds-core-config
admission:
  serviceMonitor:
    enabled: ###ZARF_VAR_SERVICE_MONITORS###
  envFrom:
    - secretRef:
        name: uds-core-config
