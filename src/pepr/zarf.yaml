kind: ZarfPackageConfig
metadata:
  name: uds-core-pepr
  description: "UDS Core Pepr Config"

components:
  - name: uds-core-pepr-config
    required: true
    charts:
      - name: uds-pepr-config
        namespace: pepr-system
        localPath: chart/
        version: 0.1.0
