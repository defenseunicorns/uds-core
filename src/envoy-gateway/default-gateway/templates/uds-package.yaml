# Copyright 2026 Defense Unicorns
# SPDX-License-Identifier: AGPL-3.0-or-later OR LicenseRef-Defense-Unicorns-Commercial

# Base network policies for whatever proxy the operator creates in this namespace. The
# operator only ever creates/deletes the Gateway object itself, on demand.
apiVersion: uds.dev/v1alpha1
kind: Package
metadata:
  name: envoy-default-gateway
  namespace: envoy-default-gateway
spec:
  network:
    allow:
      - direction: Ingress
        selector:
          app.kubernetes.io/managed-by: envoy-gateway
        remoteGenerated: Anywhere
        remoteProtocol: UDP
        description: "UDP ingress for the managed default Gateway proxy"
      - direction: Egress
        selector:
          app.kubernetes.io/managed-by: envoy-gateway
        remoteGenerated: Anywhere
        remoteProtocol: UDP
        description: "UDP egress for the managed default Gateway proxy to backends"
      - direction: Egress
        selector:
          app.kubernetes.io/managed-by: envoy-gateway
        remoteNamespace: envoy-gateway-system
        remoteSelector:
          control-plane: envoy-gateway
        port: 18000
        description: "xDS connection from the managed default Gateway proxy to the controller"
