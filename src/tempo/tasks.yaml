tasks:
  - name: validate
    actions:
      - description: Validate tempo
        wait:
          cluster:
            kind: Pod
            name: app.kubernetes.io/name=tempo
            namespace: tempo
            condition: Ready
