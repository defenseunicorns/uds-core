{{- if .Values.enabled }}
apiVersion: uds.dev/v1alpha1
kind: Exemption
metadata:
  name: pre-core-exemptions
  namespace: {{ .Values.namespace }}
spec:
  exemptions:
    {{ .Values.exemptions }}
{{- end }}
