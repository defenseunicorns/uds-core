# Copyright 2024 Defense Unicorns
# SPDX-License-Identifier: AGPL-3.0-or-later OR LicenseRef-Defense-Unicorns-Commercial

# @lulaStart 4147b6a6-3339-4d5d-b10a-f16502b52206
# @lulaStart 7db76183-fb97-4a13-ae1e-a2b45ddd4430
# @lulaStart 9fbf58ca-572f-4a20-a161-a77418dfb1ab
kind: ZarfPackageConfig
metadata:
  name: uds-core-keycloak
  description: "Keycloak"
# @lulaEnd 9fbf58ca-572f-4a20-a161-a77418dfb1ab
# @lulaEnd 7db76183-fb97-4a13-ae1e-a2b45ddd4430
# @lulaEnd 4147b6a6-3339-4d5d-b10a-f16502b52206
variables:
  - name: DOMAIN
    description: "Cluster domain"
    default: "uds.dev"

  - name: ADMIN_DOMAIN
    description: "Domain for admin services, defaults to `admin.DOMAIN`"

components:
  - name: keycloak
    required: true
    only:
      flavor: upstream
    import:
      path: common
    charts:
      - name: keycloak
        valuesFiles:
          - "values/upstream-values.yaml"
    images:
      - quay.io/keycloak/keycloak:26.5.1
      - ghcr.io/defenseunicorns/uds/identity-config:0.23.0

  - name: keycloak
    required: true
    only:
      flavor: registry1
    import:
      path: common
    charts:
      - name: keycloak
        valuesFiles:
          - "values/registry1-values.yaml"
    images:
      - registry1.dso.mil/ironbank/opensource/keycloak/keycloak:26.5.1
      - registry1.dso.mil/ironbank/opensource/defenseunicorns/uds/uds-identity-config:0.23.0

  - name: keycloak
    required: true
    only:
      flavor: unicorn
    import:
      path: common
    charts:
      - name: keycloak
        valuesFiles:
          - "values/unicorn-values.yaml"
    images:
      - quay.io/rfcurated/keycloak:26.5.1-jammy-fips-rfcurated
      - ghcr.io/defenseunicorns/uds/identity-config:0.23.0
