apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "keycloak.fullname" . }}-uds-auth-extension
  namespace: {{ .Values.namespace }}
binaryData:
  uds-auth-extension.jar: {{ .Files.Get "files/uds-auth-extension.jar" | b64enc | quote}}
