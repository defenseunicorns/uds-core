apiVersion: v1
kind: Secret
metadata:
  name: {{ include "keycloak.fullname" . }}-realm-import
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  realm.json: {{ .Files.Get "files/realm.json" | b64enc }}
