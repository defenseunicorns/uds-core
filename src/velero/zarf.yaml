kind: ZarfPackageConfig
metadata:
  name: example
  description: "Example description"

components:
  - name: test
    actions:
      onCreate:
        after:
          - cmd: echo "What can Doug do for you?"
