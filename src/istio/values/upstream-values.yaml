pilot:
  hub: "docker.io/istio"
  tag: "1.19.3-distroless"
  image: pilot
global:
  # #variant: distroless
  hub: "docker.io/istio"
  tag: "1.19.3-distroless"
  proxy_init:
    # Base name for the proxy_init container, used to configure iptables.
    image: proxyv2
  proxy:
    image: proxyv2
