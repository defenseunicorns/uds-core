{{- $ns := .Release.Namespace -}}
{{ range $tls := .Values.tls }}
{{ if ne $tls.mode "PASSTHROUGH" }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $tls.name }}
  namespace: {{ $ns }}
data:
  tls.crt: {{ $tls.cert }}
  tls.key: {{ $tls.key }}
  cacert: {{ $tls.cacert }}
type: kubernetes.io/tls
---
{{ end }}
{{ end }}
