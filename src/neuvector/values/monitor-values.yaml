leastPrivilege: false

exporter:
  enabled: true
  # Disable serviceMonitor in favor of the one provided by the UDS Package
  serviceMonitor:
    enabled: false
  apiSvc: neuvector-svc-controller-api:10443
  svc:
    enabled: true
    type: ClusterIP
