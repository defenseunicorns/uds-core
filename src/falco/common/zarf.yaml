# Copyright 2025-2026 Defense Unicorns
# SPDX-License-Identifier: AGPL-3.0-or-later OR LicenseRef-Defense-Unicorns-Commercial

kind: ZarfPackageConfig
metadata:
  name: uds-core-falco-common
  description: "UDS Core Falco Common"
  url: https://falco.org/docs/

components:
  - name: falco
    description: "Deploy Falco"
    required: true
    charts:
      - name: uds-falco-config
        namespace: falco
        version: 0.1.0
        localPath: ../chart
        valuesFiles:
          - ../chart/values.yaml
        # UDS-authored config chart for Falco rules. Exclude internal UDS-managed disabled-rule
        # lists so users cannot accidentally clear the UDS baseline suppressions.
        values:
          - sourcePath: .uds-falco-config
            targetPath: .
            excludePaths:
              - .uds-falco-config.udsDefaultDisabledRulesStable
              - .uds-falco-config.udsDefaultDisabledRulesIncubating
              - .uds-falco-config.udsDefaultDisabledRulesSandbox
      - name: falco
        url: https://falcosecurity.github.io/charts
        version: 9.1.0
        namespace: falco
        valuesFiles:
          - ../values/values.yaml
        # Falco is an upstream chart exposed broadly. Exclude image references (to prevent pulling
        # unauthorized images) and security contexts (to prevent privilege escalation).
        values:
          - sourcePath: .falco
            targetPath: .
            excludePaths:
              - .falco.image
              - .falco.falcosidekick.image
              - .falco.podSecurityContext
              - .falco.containerSecurityContext
              - .falco.falcosidekick.podSecurityContext
              - .falco.falcosidekick.securityContext
              - .falco.falcosidekick.containerSecurityContext
