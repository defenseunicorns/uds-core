service:
  labels:
    uds/istio-gateway: admin
    uds/istio-host: grafana
    uds/istio-port: "80"

sidecar:
  dashboards:
    enabled: true
    label: grafana_dashboard
  datasources:
    enabled: true
    label: grafana_datasource
