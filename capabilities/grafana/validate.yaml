tasks:
  - name: run
    actions:
      - description: Validate...
        cmd: "echo Replace Me"

        # wait:
        #   network:
        #     protocol: https
        #     address: grafana.admin.uds.dev