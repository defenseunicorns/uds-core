# UDS Core Backup & Restore

This layer of UDS Core provides backup and restore capabilities. Currently it includes the application(s):
- Velero

To deploy this layer you must also deploy its dependent layer(s):
- Base