# UDS Core Portal

This layer of UDS Core provides a user-facing application portal. Currently it includes the application(s):
- UDS Portal

To deploy this layer you must also deploy its dependent layer(s):
- Base
- Identity & Authorization
