# Copyright 2024-2026 Defense Unicorns
# SPDX-License-Identifier: AGPL-3.0-or-later OR LicenseRef-Defense-Unicorns-Commercial

kind: ZarfPackageConfig

metadata:
  name: k3d-core-slim-dev
  description: "Rehydratable UDS K3d + UDS Core Slim (Istio, UDS Operator and Keycloak) Checkpoint"
  authors: "Defense Unicorns - Product"
  # x-release-please-start-version
  version: "1.11.0"
  # x-release-please-end

variables:
  - name: CLUSTER_NAME
    description: "Name of the cluster"
    default: "uds"
  - name: K3D_EXTRA_ARGS
    description: "Optionally pass k3d arguments to the default"
    default: ""
  - name: NGINX_EXTRA_PORTS
    description: "Optionally allow more ports through Nginx (combine with K3D_EXTRA_ARGS '-p <port>:<port>@server:*')"
    default: "[]"

components:
  - name: destroy-cluster
    required: true
    description: "Optionally destroy the cluster before creating it"
    actions:
      onDeploy:
        before:
          - cmd: k3d cluster delete ${ZARF_VAR_CLUSTER_NAME}
            description: "Destroy the cluster"
          - cmd: |
              docker volume rm "k3d-${ZARF_VAR_CLUSTER_NAME}-k3s"     >/dev/null 2>&1 || true
              docker volume rm "k3d-${ZARF_VAR_CLUSTER_NAME}-kubelet" >/dev/null 2>&1 || true
            description: "Remove checkpoint volumes"

  - name: create-cluster
    required: true
    description: "Create the K3d cluster w/UDS Core pre-installed"
    files:
      - source: uds-checkpoint.tar
        target: uds-checkpoint.tar
      - source: busybox.tar
        target: busybox.tar
    actions:
      onCreate:
        before:
          - description: "Quiesce checkpoint dependencies: Keycloak, Pepr watcher, Pepr admission, then ztunnel"
            cmd: |
              set -e
              ./zarf tools kubectl --context k3d-uds -n keycloak scale statefulset keycloak --replicas=0
              ./zarf tools kubectl --context k3d-uds -n keycloak wait --for=delete pod -l app.kubernetes.io/name=keycloak,app.kubernetes.io/instance=keycloak --timeout=60s

              ./zarf tools kubectl --context k3d-uds -n pepr-system scale deploy pepr-uds-core-watcher --replicas=0
              ./zarf tools kubectl --context k3d-uds -n pepr-system wait --for=delete pod -l app=pepr-uds-core-watcher --timeout=60s

              ./zarf tools kubectl --context k3d-uds -n pepr-system scale deploy pepr-uds-core --replicas=0
              ./zarf tools kubectl --context k3d-uds -n pepr-system wait --for=delete pod -l app=pepr-uds-core --timeout=60s

              ./zarf tools kubectl --context k3d-uds -n istio-system patch daemonset ztunnel --type='strategic' -p '{"spec":{"template":{"spec":{"nodeSelector":{"no-such-label":"true"}}}}}'
              ./zarf tools kubectl --context k3d-uds -n istio-system wait --for=delete pod -l app=ztunnel --timeout=60s

              ./checkpoint.sh
            shell:
              linux: bash
              darwin: bash
        onSuccess:
          - cmd: rm -f uds-checkpoint.tar busybox.tar
      onDeploy:
        after:
          - cmd: |
              set -euo pipefail
              VOL_K3S="k3d-${ZARF_VAR_CLUSTER_NAME}-k3s"
              VOL_KUBELET="k3d-${ZARF_VAR_CLUSTER_NAME}-kubelet"

              # Load the checkpoint image under its known tag (same as in checkpoint.sh) so
              # k3d uses the bundled image instead of pulling it from a registry.
              CHECKPOINT_IMAGE="ghcr.io/defenseunicorns/uds-core/checkpoint:latest"
              tar -xOf uds-checkpoint.tar uds-k3d-checkpoint-latest.tar | docker load >/dev/null
              docker load < busybox.tar >/dev/null

              # Create named Docker volumes and inject the checkpoint data by
              # piping each tarball from the bundle directly into a busybox
              # helper container that mounts the volume.
              #
              # Named volumes live inside the Docker VM, so this works on macOS
              # (Lima, OrbStack, Docker Desktop) — no host bind-mount or sudo
              # required.
              docker volume create "$VOL_K3S" >/dev/null
              docker volume create "$VOL_KUBELET" >/dev/null

              tar -xOf uds-checkpoint.tar k3s_data.tar | \
                docker run --rm -i --user root -v "${VOL_K3S}:/data" cgr.dev/chainguard/busybox tar -x -C /data -f -

              tar -xOf uds-checkpoint.tar kubelet_data.tar | \
                docker run --rm -i --user root -v "${VOL_KUBELET}:/data" cgr.dev/chainguard/busybox tar -x -C /data -f -

              # Read the cluster token from the restored volume — no host
              # filesystem access needed.
              K8S_TOKEN=$(docker run --rm \
                --user root -v "${VOL_K3S}:/data" cgr.dev/chainguard/busybox \
                cat /data/server/token)

              # Create the cluster using named volumes instead of host paths.
              # k3d's -v flag accepts volume-name:/path just like docker run.
              k3d cluster create \
                -p "80:80@server:*" \
                -p "443:443@server:*" \
                --api-port 6550 \
                --k3s-arg "--disable=traefik@server:*" \
                --k3s-arg "--disable=metrics-server@server:*" \
                --k3s-arg "--disable=servicelb@server:*" \
                --k3s-arg "--disable=local-storage@server:*" \
                --k3s-arg "--token=${K8S_TOKEN}@server:*" \
                -v "${VOL_K3S}:/var/lib/rancher/k3s@server:*" \
                -v "${VOL_KUBELET}:/var/lib/kubelet@server:*" \
                --image "$CHECKPOINT_IMAGE" \
                ${ZARF_VAR_K3D_EXTRA_ARGS} \
                ${ZARF_VAR_CLUSTER_NAME}
            shell:
              linux: bash
              darwin: bash
            description: "Create the cluster"

          - description: "Restore checkpoint dependencies: ztunnel, Pepr admission, Pepr watcher, then Keycloak"
            cmd: |
              set -e
              ./zarf tools kubectl --context k3d-${ZARF_VAR_CLUSTER_NAME} -n istio-system patch daemonset ztunnel --type=json -p='[{"op":"remove","path":"/spec/template/spec/nodeSelector/no-such-label"}]'
              ./zarf tools kubectl --context k3d-${ZARF_VAR_CLUSTER_NAME} -n istio-system rollout status daemonset ztunnel --timeout=60s

              ./zarf tools kubectl --context k3d-${ZARF_VAR_CLUSTER_NAME} -n pepr-system scale deploy pepr-uds-core --replicas=2
              ./zarf tools kubectl --context k3d-${ZARF_VAR_CLUSTER_NAME} -n pepr-system rollout status deploy pepr-uds-core --timeout=60s

              ./zarf tools kubectl --context k3d-${ZARF_VAR_CLUSTER_NAME} -n pepr-system scale deploy pepr-uds-core-watcher --replicas=1
              ./zarf tools kubectl --context k3d-${ZARF_VAR_CLUSTER_NAME} -n pepr-system rollout status deploy pepr-uds-core-watcher --timeout=60s

              ./zarf tools kubectl --context k3d-${ZARF_VAR_CLUSTER_NAME} -n keycloak scale statefulset keycloak --replicas=1
              ./zarf tools kubectl --context k3d-${ZARF_VAR_CLUSTER_NAME} -n keycloak rollout status statefulset keycloak --timeout=180s

          # Wait on Keycloak since it is the slowest pod to start after cluster
          # creation.
          - description: Keycloak to be Healthy
            wait:
              cluster:
                kind: Pod
                name: app.kubernetes.io/name=keycloak
                namespace: keycloak
                condition: Ready
          - description: Pepr Admission to be Healthy
            wait:
              cluster:
                kind: Pod
                name: app=pepr-uds-core
                namespace: pepr-system
                condition: Ready
          - description: Pepr Watcher to be Healthy
            wait:
              cluster:
                kind: Pod
                name: app=pepr-uds-core-watcher
                namespace: pepr-system
                condition: Ready
        onSuccess:
          - cmd: rm -f uds-checkpoint.tar busybox.tar
