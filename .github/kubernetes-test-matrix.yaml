# Copyright 2026 Defense Unicorns
# SPDX-License-Identifier: AGPL-3.0-or-later OR LicenseRef-Defense-Unicorns-Commercial

# Supported Kubernetes minors: https://kubernetes.io/releases/
kubernetesVersions:
  # Initial set only: these are the only matrix versions currently backed by the required uds-k3d custom images.
  # renovate: datasource=github-releases depName=k3s-io/k3s versioning=regex:^v(?<major>\d+)\.(?<minor>\d+)\.(?<patch>\d+)\+k3s(?<build>\d+)$
  - version: "v1.34.4+k3s1"
  # renovate: datasource=github-releases depName=k3s-io/k3s versioning=regex:^v(?<major>\d+)\.(?<minor>\d+)\.(?<patch>\d+)\+k3s(?<build>\d+)$
  - version: "v1.35.8+k3s1"
flavors:
  - upstream
  - registry1
  - unicorn
