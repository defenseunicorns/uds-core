# Copyright 2026 Defense Unicorns
# SPDX-License-Identifier: AGPL-3.0-or-later OR LicenseRef-Defense-Unicorns-Commercial

name: CLI Compatibility

on:
  schedule:
    # Runs every morning at 2:00 AM UTC
    - cron: "0 2 * * *"
  pull_request:
    # milestoned is added here as a workaround for release-please not triggering PR workflows (PRs should be added to a milestone to trigger the workflow).
    # labeled is added to support renovate-ready labelling on PRs
    types: [milestoned, labeled, opened, reopened, synchronize]
    paths:
      - ".github/workflows/test-cli-matrix.yaml"
      - "bundles/k3d-standard/**"
      - "bundles/k3d-standard-next/**"

# Abort prior jobs in the same workflow / PR
concurrency:
  group: test-cli-matrix-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write # This is needed for OIDC federation.
  packages: read # Allows reading the published GHCR packages

jobs:
  cli-upgrade:
    runs-on: uds-ubuntu-big-boy-8-core
    timeout-minutes: 60
    name: "Test Upgrade (${{ matrix.mode }}, ${{ matrix.scenario }}, ${{ matrix.flavor }})"

    strategy:
      matrix:
        include:
          - mode: legacy
            scenario: legacy-old-old
            flavor: upstream
            install_cli: v0.34.0 # Minimum supported Legacy CLI
            upgrade_cli: v0.34.0
          - mode: legacy
            scenario: legacy-old-new
            flavor: upstream
            install_cli: v0.34.0 # Minimum supported Legacy CLI
            # renovate: datasource=github-tags depName=defenseunicorns/uds-cli versioning=semver
            upgrade_cli: v0.37.0
          - mode: next
            scenario: legacy-to-next
            flavor: upstream
            install_cli: v0.34.0 # Minimum supported Legacy CLI
            upgrade_cli: v0.36.0 # Minimum supported CLI Next version
          # TODO: Uncomment this scenario after the first Next bundle is published.
          # - mode: next
          #   scenario: next-min-to-next-latest
          #   flavor: upstream
          #   install_cli: v0.36.0 # Minimum supported CLI Next version
          #   # renovate: datasource=github-tags depName=defenseunicorns/uds-cli versioning=semver
          #   upgrade_cli: v0.37.0

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Initialize CI timing
        uses: ./.github/actions/ci-timing
        continue-on-error: true
        env:
          CI_TIMING_JOB_KIND: cli-upgrade
          CI_TIMING_PACKAGE: all
          CI_TIMING_FLAVOR: ${{ matrix.flavor }}
          CI_TIMING_TEST_TYPE: upgrade
          CI_TIMING_SCENARIO: ${{ matrix.scenario }}
        with:
          mode: init

      - name: Environment setup
        uses: ./.github/actions/setup
        with:
          ghToken: ${{ secrets.GITHUB_TOKEN }}
          installK3d: 'true'

      - name: Install CLI for install phase
        uses: defenseunicorns/setup-uds@d6add59d4c2ca789ed7c92741e91bd340b4e950b # v1.0.2
        with:
          version: ${{ matrix.install_cli }}

      - name: Print CLI version for debug
        run: uds version

      - name: Deploy latest release (install phase)
        run: bash .github/actions/ci-timing/timing.sh run latest-release-deploy uds run -f tasks/deploy.yaml latest-bundle-release --set FLAVOR=${{ matrix.flavor }} --no-progress

      - name: Deploy latest release test resources
        run: bash .github/actions/ci-timing/timing.sh run latest-release-test-resources uds run -f tasks/deploy.yaml latest-release-test-resources --set FLAVOR=${{ matrix.flavor }} --no-progress

      - name: Install CLI for upgrade phase
        uses: defenseunicorns/setup-uds@d6add59d4c2ca789ed7c92741e91bd340b4e950b # v1.0.2
        with:
          version: ${{ matrix.upgrade_cli }}

      - name: Print CLI version for debug
        run: uds version

      - name: Run Legacy upgrade phase
        if: ${{ matrix.mode == 'legacy' }}
        run: bash .github/actions/ci-timing/timing.sh run cli-upgrade uds run -f tasks/test.yaml upgrade-existing-legacy --set FLAVOR=${{ matrix.flavor }} --no-progress

      - name: Run Next upgrade phase
        if: ${{ matrix.mode == 'next' }}
        run: bash .github/actions/ci-timing/timing.sh run cli-upgrade uds run -f tasks/test.yaml upgrade-existing --set FLAVOR=${{ matrix.flavor }} --no-progress

      - name: Debug Output
        if: ${{ always() }}
        uses: ./.github/actions/debug-output

      - name: Save logs
        if: always()
        uses: ./.github/actions/save-logs
        with:
          suffix: -upgrade-matrix-${{ matrix.mode }}-${{ matrix.scenario }}-${{ matrix.flavor }}

      - name: Finalize CI timing
        if: ${{ always() }}
        uses: ./.github/actions/ci-timing
        continue-on-error: true
        with:
          mode: finalize
          status: ${{ job.status }}
          artifact_name: ci-timing-cli-upgrade-${{ matrix.mode }}-${{ matrix.scenario }}-${{ matrix.flavor }}-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Send Slack notification
        if: failure() && github.event_name == 'schedule'
        uses: ./.github/actions/slack-alert
        with:
          webhook-url: ${{ secrets.SLACK_WEBHOOK_URL }}
          channel: ${{ vars.SLACK_ALERT_CHANNEL }}

  cli-install:
    runs-on: uds-ubuntu-big-boy-8-core
    timeout-minutes: 30
    name: "Test Install (${{ matrix.mode }}, minimum supported, upstream)"

    strategy:
      matrix:
        include:
          - mode: legacy
            cli_version: v0.34.0 # Minimum supported Legacy CLI
            task: test:uds-core-legacy-e2e
          - mode: next
            cli_version: v0.36.0 # Minimum supported CLI Next version
            task: test:uds-core-e2e

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Initialize CI timing
        uses: ./.github/actions/ci-timing
        continue-on-error: true
        env:
          CI_TIMING_JOB_KIND: cli-install
          CI_TIMING_PACKAGE: all
          CI_TIMING_FLAVOR: upstream
          CI_TIMING_TEST_TYPE: install
          CI_TIMING_SCENARIO: ${{ matrix.mode }}-minimum-supported
        with:
          mode: init

      - name: Environment setup
        uses: ./.github/actions/setup
        with:
          ghToken: ${{ secrets.GITHUB_TOKEN }}
          installK3d: 'true'

      - name: Install minimum supported CLI
        uses: defenseunicorns/setup-uds@d6add59d4c2ca789ed7c92741e91bd340b4e950b # v1.0.2
        with:
          version: ${{ matrix.cli_version }}

      - name: Print CLI version for debug
        run: uds version

      - name: Test UDS Core Install (minimum supported CLI)
        run: bash .github/actions/ci-timing/timing.sh run cli-install uds run ${{ matrix.task }} --set FLAVOR=upstream --no-progress

      - name: Debug Output
        if: ${{ always() }}
        uses: ./.github/actions/debug-output

      - name: Save logs
        if: always()
        uses: ./.github/actions/save-logs
        with:
          suffix: -install-${{ matrix.mode }}-min-cli-upstream

      - name: Finalize CI timing
        if: ${{ always() }}
        uses: ./.github/actions/ci-timing
        continue-on-error: true
        with:
          mode: finalize
          status: ${{ job.status }}
          artifact_name: ci-timing-cli-install-${{ matrix.mode }}-upstream-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Send Slack notification
        if: failure() && github.event_name == 'schedule'
        uses: ./.github/actions/slack-alert
        with:
          webhook-url: ${{ secrets.SLACK_WEBHOOK_URL }}
          channel: ${{ vars.SLACK_ALERT_CHANNEL }}
