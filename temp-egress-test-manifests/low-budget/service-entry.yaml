apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: service-entry-1
  namespace: ns-1
spec:
  location: MESH_EXTERNAL
  hosts:
  - example.com
  ports: 
  - number: 443
    name: tls
    protocol: TLS
  resolution: DNS
  exportTo:
    - "."