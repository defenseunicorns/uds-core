apiVersion: uds.dev/v1alpha1
kind: Package
metadata:
  name: test
  namespace: default
spec:
  network:
    allow:
      - direction: Ingress
        remoteNamespace: "kube-system"
        remoteSelector:
          k8s-app: kube-dns
        port: 9153
        selector:
          app.kubernetes.io/name: prometheus
        port: 9090
        description: "CoreDNS Metrics Queries"
